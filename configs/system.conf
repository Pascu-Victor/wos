#!/bin/bash
# configs/system.conf - System identity configuration
# Source this file from build scripts.
#
# Changing values here invalidates the initramfs cache and triggers a rebuild.

# Default hostname for standalone (non-cluster) builds.
# cluster_setup.py overrides this per-node (e.g. wos-0, wos-1, ...).
WOS_HOSTNAME="wos"
